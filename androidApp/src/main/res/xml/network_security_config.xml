<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Camera APIs (Sony JSON-RPC, Canon CCAPI, Panasonic cam.cgi) communicate
      over plain HTTP on local Wi-Fi Direct networks. These embedded camera
      servers do not support HTTPS.

      Cleartext is disabled globally. Known camera gateway IPs are allowlisted
      below. Dynamic IPs not listed here still work because Android's
      domain-config applies at the platform level, while camera HTTP traffic
      routes through OkHttp with CameraNetworkBinding (which bypasses the
      platform cleartext check for bound network sockets).
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Camera gateway IPs: allow cleartext for known Wi-Fi Direct subnets -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.1</domain>
        <domain includeSubdomains="false">192.168.122.1</domain>
        <domain includeSubdomains="false">192.168.49.1</domain>
        <domain includeSubdomains="false">10.0.0.1</domain>
    </domain-config>

    <!-- Debug builds can additionally use user-installed CAs -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
